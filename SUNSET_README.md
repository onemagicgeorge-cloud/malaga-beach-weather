# Sunset Forecast Bot — Уельва

Прогнозує ймовірність ефектного заходу сонця і постить у Telegram-канал.
Побудовано поверх репо `malaga-beach-weather` (той самий стиль, ті самі
секрети `BOT_TOKEN` / `CHANNEL_ID`).

## Що робить скрипт

1. **Азимут заходу сонця** рахується локально (без астрономічних API) за
   схиленням Сонця та часовим кутом на заході
   (формула Спенсера + атмосферна рефракція −0.833°).
   → ~270° на рівнодення, ~300° влітку, ~241° взимку.

2. **Точка Б** ("горизонт"/"прожектор") — від Точки А (де знімаєш)
   відкладаються дві точки: 40 км і 110 км у бік цього азимута
   (формула great-circle destination point).

3. **Два запити до Open-Meteo** (обидва з `timezone=UTC`, бо Точка Б
   буває над морем, де auto може збити годинну сітку):
   - Точка А (набережна/порт у центрі Уельви, 37.2600, -6.9504):
     `cloud_cover_mid` + `cloud_cover_high` — "екран", ідеал 30–70%.
   - Точки горизонту: `cloud_cover_low` — "прожектор" (≤15% — повний бал,
     ≥60% — нуль).

4. **Індекс = (оцінка екрану × оцінка прожектора) / 100.**
   Добуток, а не сума: без чистого "прожектора" нижні хмари не дадуть
   кольору, хоч які б ідеальні хмари були зверху.

5. **Усереднення** — беруться `AVG_HOURS` (за замовч. 3) годин, найближчих
   до заходу сонця, щоб згладжувати шум і зменшити хибні тривоги.

6. **Сповіщення** (`MIN_INDEX_TO_NOTIFY = 30`, нижче — не постимо):
   - 🟡 30–50% — "Є непоганий шанс на цікаве небо..."
   - 🟠 50–75% — "Хороші умови! Готуй камеру."
   - 🔴 75–100% — "Епічне небо на підході! Піднімай дрон."

## Що можна підлаштувати

- **`LATITUDE` / `LONGITUDE`** — точка знімання (зараз набережна в центрі
  Уельви). Якщо знімаєш деінде — зміни.
- **`HORIZON_NEAR_KM` / `HORIZON_FAR_KM`** (за замовч. 40/110) — гратись,
  чи 60/160 км дають точніший прогноз.
- **Вікно усереднення** — скільки годин навколо заходу усереднювати
  (зараз `[-4h, +3h]`). Більше — стабільніше, менше — чутливіше.
- Формула азимута — локальна астрономічна (див. `sunset_azimuth()`).

## Встановлення

1. `sunset_forecast_bot.py` — у корінь репозиторію.
2. `sunset_forecast.yml` — у `.github/workflows/`.
3. Секрети `BOT_TOKEN` уже є. Додай секрет `SUNSET_CHANNEL_ID` (ID нового
   Telegram-каналу) — прогнози заходу сонця йдуть саме туди.
4. Залежність — тільки `requests` (встановлюється у workflow).
5. Перевірити вручну: вкладка Actions → Sunset Forecast → Run workflow.

## Про cron

`0 13 * * *` (13:00 UTC щодня) — запас 3–6+ год до заходу сонця цілий рік.
